namespace clustering.Enums
{
    public enum FileTypes
    {
        //Enum:en FileTypes innehåller en metod för att mappa våran klassificering till ett numeriskt värde
        Executable = 0,
        Document = 1,
        Script = 2
    }
}